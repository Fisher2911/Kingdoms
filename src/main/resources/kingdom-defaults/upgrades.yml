upgrades:
  1:
    type: int
    id: max-claims
    display-name: '<blue>Max Claims'
    expression: "level * 5"
    money-price-expression: "level * 100"
    max-level: 10
  2:
    type: int
    id: max-members
    display-name: '<blue>Max Members'
    expression: "level * 2"
    money-price-expression: "level * 100"
    max-level: 5
  3:
    type: double
    id: bank-limit
    display-name: '<blue>Bank Limit'
    expression: "level * 100000"
    money-price-expression: "level * 25000"
    max-level: 10
  4:
    type: int
    id: max-allies
    display-name: '<blue>Max Allies'
    expression: "level * 1"
    money-price-expression: "level * 1000"
    max-level: 3
  5:
    type: int
    id: max-truces
    display-name: '<blue>Max Truces'
    expression: "level * 1"
    money-price-expression: "level * 1000"
    max-level: 4
  6:
    type: int
    id: max-enemies
    display-name: '<blue>Max Enemies'
    expression: "level * 1"
    money-price-expression: "level * 1000"
    max-level: 4